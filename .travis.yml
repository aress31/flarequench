language: java

before_cache:
- rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
- rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/

deploy:
  provider: releases
  github_token: $GITHUB_TOKEN
  file: build/libs/flarequench-all.jar
  skip_cleanup: true
  on:
    branch: master
    tags: true

jdk:
  - oraclejdk10
  - oraclejdk11

os:
- linux
- osx
- windows